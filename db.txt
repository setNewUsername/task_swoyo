CREATE TABLE token_url_pare (
    id    integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    full_url varchar(2048) NOT NULL,
    token varchar(50) NOT NULL,
    token_timestamp integer NOT NULL
);